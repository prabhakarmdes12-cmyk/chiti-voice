# TEMPORARY — CI diagnostics only. DELETE THIS FILE once the workspace is green.
# See scripts/ci-rustc-capture.sh and scripts/ci-test-capture.sh for why they exist.

[build]
# Runner-absolute path: this breaks non-Linux builds, which is acceptable while the
# diagnostics are in place and is the reason the file must not survive.
rustc-wrapper = "/home/runner/work/chiti-voice/chiti-voice/scripts/ci-rustc-capture.sh"

[target.x86_64-unknown-linux-gnu]
runner = "scripts/ci-test-capture.sh"
