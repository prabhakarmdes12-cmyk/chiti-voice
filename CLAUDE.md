# Chiti Vocal Runtime — agent pointer

Read [`AGENTS.md`](./AGENTS.md) in this directory. It is the single agent-facing document: what exists, what is
only specified, the commands to verify, the contracts that bite, and the rules this repo has learned the hard way.

This pointer file exists so that agents configured to open `CLAUDE.md` land on the same text. It is deliberately
not a copy: duplicated instructions drift, and the drift is always in the copy nobody reads.
